function ProjectCards(props){
    return (
        <div className="ProjectCards" style={props.style}>
            <h1 className="titulo">{props.texto}</h1>
        </div>
    )
}

export default ProjectCards